[pytest]
markers =
    live: marks tests as live integration tests requiring real external API network calls (deselect with -m "not live")
addopts = -m "not live"
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
